# Quick Run CPU Version

⚠️ Does NOT require GPU

## Setup

```bash
# Create virtual environment
python == 3.10 
# Activate


# Install dependencies
pip install -r requirements_cpu.txt 
streamlit run app_cpu.py 
