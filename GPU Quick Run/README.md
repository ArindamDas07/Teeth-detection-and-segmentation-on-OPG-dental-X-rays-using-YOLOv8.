# Quick Run GPU Version

⚠️ Requires NVIDIA GPU

## Setup

```bash
# Create virtual environment
python == 3.10 
# Activate


# Install dependencies
pip install -r requirements_gpu.txt 
streamlit run app_gpu.py 
